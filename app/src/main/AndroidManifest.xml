<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.derrick.cart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBasic">
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ItemsActivity"
            android:label="@string/title_activity_items" />
        <activity
            android:name=".SubItemsActivity"
            android:exported="false"
            android:label="@string/title_activity_sub_items"
            android:parentActivityName=".ItemsActivity"
            android:theme="@style/Theme.AppBasic.NoActionBar" />
        <activity
            android:name=".SubItemActivity"
            android:label="@string/note_activity_label"
            android:parentActivityName=".SubItemsActivity" />
    </application>

</manifest>